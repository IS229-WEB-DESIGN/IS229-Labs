(async()=>{const c=document.querySelector('[data-featured-destinations]');const s=document.querySelector('[data-home-status]');if(!c)return;try{const r=await fetch('data/destinations.json');if(!r.ok)throw new Error(`HTTP ${r.status}`);const data=await r.json();c.innerHTML=data.slice(0,3).map(card).join('');if(s)s.textContent='Featured destinations loaded.';}catch(e){if(s)s.textContent='Could not load destination data. Run through a local server.';}function card(d){return `<article class="card"><img src="${d.img}" alt="Illustrated scene for ${d.name}" width="1200" height="520"><div class="card-body"><span class="pill">${d.category}</span><h3>${d.name}</h3><p>${d.tagline}</p><p><strong>${d.location}</strong> · From K${d.price}</p></div></article>`;}})();
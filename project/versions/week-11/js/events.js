(async()=>{const list=document.querySelector('[data-event-list]');const status=document.querySelector('[data-event-status]');if(!list)return;try{const r=await fetch('data/events.json');if(!r.ok)throw new Error();const events=await r.json();list.innerHTML=events.map(e=>`<article class="event-card"><span class="pill">${e.type}</span><h2>${e.title}</h2><p><strong>${e.date}</strong> · ${e.location}</p><p>${e.description}</p></article>`).join('');status.textContent=`${events.length} events loaded.`;}catch(e){status.textContent='Events could not be loaded.';}})();
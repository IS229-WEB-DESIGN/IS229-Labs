# Week 06 - JavaScript & DOM Interaction

Start from: Week 05 produced a responsive interface ready for interaction.

Completed outcome: Event listeners, state changes, class toggling and interactive mobile menus

Run: open `index.html` in a browser. For Fetch/API weeks, use `python -m http.server 8000`.

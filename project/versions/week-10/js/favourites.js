(() => {const key='morobe-favorites';const read=()=>JSON.parse(localStorage.getItem(key)||'[]');document.querySelectorAll('[data-favorite]').forEach(button=>button.addEventListener('click',()=>{const id=button.dataset.favorite;let items=read();items=items.includes(id)?items.filter(x=>x!==id):[...items,id];localStorage.setItem(key,JSON.stringify(items));const saved=items.includes(id);button.textContent=saved?'★ Saved':'☆ Save';button.setAttribute('aria-pressed',String(saved));}));})();